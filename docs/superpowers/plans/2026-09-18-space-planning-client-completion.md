# Space-planning Client completion — plan

Spec: [design](../specs/2026-09-18-space-planning-client-completion-design.md). Initial dirty work captured in `/tmp/lisno-space-planning-approval-qa/initial-{status.txt,tracked.diff,untracked.tar.gz}`; preserve all unrelated changes.

1. Diagnose existing approvals and stage projection — complete. Aggregate image approval already finalizes Design; displayed stage independently follows floor tasks.
2. Implement backend/source and frontend consumers in parallel — complete. Backend owner: additive repository source, workflow action/domain/service/projection/OpenAPI and focused backend regressions. Frontend owner: workflow API type, shared completion UI, stage and Client image review integration and tests. Root owns contract/documents/browser integration. Root's final UI adjustment uses a labelled group for the shared control so both entry points can coexist without duplicate page landmarks.
3. Integrated integrity review — complete; no remaining actionable findings. Preserved immutable approval lineage, actual Client identity, source races, finance/task deduplication and legacy projection. Review corrections isolate the validated canonical estimate from unrelated historical review rounds and reject malformed representative-proof dates.
4. Final verification — complete; independent evidence/preservation audit passed. Initial 79 dirty paths became 92, with no initial path removed; product changes stayed within the assigned workflow scope and prior unrelated changes were preserved.
5. Handoff — local only; no deploy/commit/migration.

## Implementation and verification evidence

The sixth stage uses an explicit Client acknowledgement of the approved estimate/plan/version/round. Existing per-image approval still finalizes Design and generates finance/tasks at the same time as before; this new action never repeats that work. Acknowledgement remains transactionally scoped, authorized, version-checked and idempotent. The backend preserves floor-only legacy projects, source conflicts fail closed, and no schema migration or dependency is required.

The shared `SpacePlanningCompletion` is available in the project stage and the expanded approved estimate. No polling was added. Pending/partial review shows counts; backend eligibility controls the final button. Confirmation captures the semantic source and workflow version. Changed source, lost authorization, refresh failure and CAS conflicts prevent submission. Success refreshes affected project/Client queries. Existing generic workflow refresh behavior is preserved.

Passing focused coverage contains **620 unique tests: 430 backend and 190 frontend**. Repeated test runs are not counted twice.

- Backend: `npm test -- tests/workflow-space-planning.test.ts tests/workflow-space-planning.replica-set.test.ts tests/design-workflow-state.test.ts tests/design-workflow-projection.test.ts tests/design-workflow-submission-gates.test.ts tests/project-workflow.test.ts tests/project-workflow-routes.test.ts tests/api-docs.test.ts` — 428 passed before the two final review regressions; follow-up `npm test -- tests/workflow-space-planning.test.ts tests/workflow-space-planning.replica-set.test.ts tests/api-docs.test.ts` — 87 passed, including 51 source/action tests and 12 Mongo replica tests. The 430 unique backend cases cover actual last-image finalization, unchanged finance/task side effects, canonical pin isolation, malformed evidence, wrong Client/project/payload, prerequisites, pause, CAS, replay, concurrency, rollback and a deterministic source-read race.
- Frontend: 190 tests in the 12 affected suites (`frontend-focused-tests.log`). Final `npm test -- src/features/workflow/SpacePlanningCompletion.test.tsx` — 25 passed after the landmark correction, including dual-entry-point accessibility, keyboard, source changes, refresh/retry, exact request and repeat-click behavior.
- Both workspaces: `npm run typecheck` and `npm run build` passed. `git diff --check` passed. No lint script exists.
- Rendered QA uses real components with synthetic API transport at widths 360, 768 and 1440: partial review hides final action; last-image approval reveals both controls without reload; exact final request completes both views with one stage action. Changed source and failed refresh block confirmation; retry recovers. New stage UI has no horizontal overflow or axe violations at all widths. Mobile confirmation has no axe violations once its opening animation settles. Both surfaces together exposed duplicate landmarks, which were corrected and rechecked. Browser console: 0 errors/warnings.

Evidence and screenshots: `/tmp/lisno-space-planning-approval-qa/`, especially `backend-final-regression.log`, `backend-review-regression.log`, `frontend-focused-tests.log`, `frontend-final-accessibility.log`, `browser-final.log`, `browser-final-verified.log`, `browser-dialog-settled.log`, and `final-ready-{360,768,1440}.png`, `final-confirm-mobile.png`, `final-completed-mobile.png`. Browser and preview were stopped; the temporary dependency symlink was removed.

Limits: full repository suites and live production/browser verification were not run. Existing Vite large-bundle warnings and Mongoose option deprecation warnings remain. Full combined-page axe also reports existing estimate-summary/small-text contrast issues outside the new completion UI; these were not changed. No deployment, commit, production mutation, write migration, email or customer communication was performed.
